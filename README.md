# Duplicate Question Detection App

ML + NLP application using **TF-IDF and Linear SVM** to identify duplicate question pairs, achieving **80.6% accuracy**, with an interactive **Streamlit** interface.
